<resources>
    <integer name="directory_columns_vertical_scroll">2</integer>
    <integer name="directory_columns_horizontal_scroll">3</integer>
    <integer name="media_columns_vertical_scroll">3</integer>
    <integer name="media_columns_horizontal_scroll">4</integer>

    <integer name="default_sorting">33794</integer>

    <!-- how far a single tag value is allowed to run before it is cut short. An embedded XMP packet
         or a maker note is thousands of characters and would bury the rows under it; a long-press
         still copies the value in full -->
    <integer name="metadata_value_max_lines">12</integer>
</resources>
