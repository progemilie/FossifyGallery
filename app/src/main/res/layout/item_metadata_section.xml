<?xml version="1.0" encoding="utf-8"?>
<!--
    A collapsible group: the tappable heading, and under it the container its rows are built into
    the first time it is opened.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <LinearLayout
        android:id="@+id/metadata_section_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/selectableItemBackground"
        android:gravity="center_vertical"
        android:minHeight="@dimen/metadata_section_header_height"
        android:orientation="horizontal"
        android:paddingHorizontal="@dimen/normal_margin"
        android:paddingVertical="@dimen/small_margin">

        <TextView
            android:id="@+id/metadata_section_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textSize="@dimen/normal_text_size"
            android:textStyle="bold"
            tools:ignore="TextContrastCheck"
            tools:text="Exif SubIFD" />

        <TextView
            android:id="@+id/metadata_section_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingHorizontal="@dimen/small_margin"
            android:textSize="@dimen/normal_text_size"
            tools:ignore="TextContrastCheck"
            tools:text="34" />

        <ImageView
            android:id="@+id/metadata_section_chevron"
            android:layout_width="@dimen/normal_icon_size"
            android:layout_height="@dimen/normal_icon_size"
            android:contentDescription="@null"
            android:src="@drawable/ic_chevron_down_vector" />

    </LinearLayout>

    <LinearLayout
        android:id="@+id/metadata_section_body"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingHorizontal="@dimen/normal_margin"
        android:paddingBottom="@dimen/medium_margin"
        android:visibility="gone" />

</LinearLayout>
